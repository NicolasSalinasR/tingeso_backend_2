spring:
  application:
    name: HU1

  datasource:
    url: jdbc:postgresql://localhost:5432/HU1
    username: postgres
    password: conan2084
    driver-class-name: org.postgresql.Driver  # Asegúrate de que esta línea esté incluida

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
server:
  port: 8082

